// Copy to config.local.js next to app.json and put the Neo device token here.
// The file is ignored by git but packaged into the AIX, so the glasses can connect.
export default { deviceToken: '' };
